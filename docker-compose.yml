version: "3"

services:

  grpc-server:
    image: grpc:server
    build: ./grpc-server
    expose:
      - "7777"
    depends_on:
      - envoy

  envoy:
    image: envoy:sidecar
    build: ./envoy
    ports: 
      - "8080:8080"
      
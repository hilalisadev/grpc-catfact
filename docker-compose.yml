version: "3"

services:

  grpc-server:
    image: grpc:server
    build: ./grpc-server
    ports:
      - "7777:7777"

  envoy:
    image: envoy:sidecar
    build: ./envoy
    ports: 
      - "8080:8080"
    links:
      - grpc-server
